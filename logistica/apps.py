from django.apps import AppConfig

class LogisticaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'logistica'
    verbose_name = "LOGÍSTICA" # Esto es lo que verá el usuario
